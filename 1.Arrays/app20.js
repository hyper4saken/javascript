//  The sort() methods sorts the elements of an array in place and returns the sorted array. The default sort order is build upon converting the elements into strings, them comparing their sequences of UTF-16 code unites values

let people = ["Mrs. Robsinson", "Angie", "Jolene", "Maggie May", "Roxanne"];
console.log(people);

//It sorts an array
console.log(people.sort());

let nums = [34, 10, 10000, 67, 67, 99];
console.log(nums);

// sorring numbers
console.log(nums.sort());
